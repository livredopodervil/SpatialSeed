{
  "name": "spatial-seed-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "audit:web-entrypoints": "python3 tools/audit_web_entrypoints.py"
  },
  "description": "Monorepo sem build obrigatório; módulos ES nativos no navegador."
}
