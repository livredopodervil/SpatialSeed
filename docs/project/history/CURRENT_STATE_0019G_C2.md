# Snapshot histórico - 0019g-c2

Este conteúdo ocupava anteriormente `docs/project/CURRENT_STATE.md`. Foi movido
para preservar o registro sem continuar sendo confundido com o estado atual.

Build funcional registrado: `20260713-0019g-c2`.

## Confirmado naquele marco

- seleção única e múltipla;
- pivô e gizmo;
- duplicação em lote;
- duplicação afim cumulativa;
- `repeat` por matriz delta;
- geometria, materiais e texturas compartilhados;
- renderer incremental;
- `THREE.InstancedMesh`;
- picking por `instanceId`;
- highlight por `instanceColor`;
- limites espaciais atualizados apenas nos lotes alterados.

## Testes registrados naquele marco

`49 aprovados; 0 falhas`.

## Pendências registradas naquele marco

- expressões no console;
- vírgula decimal;
- tolerância de toque;
- grupos hierárquicos;
- distribuição pública/offline;
- navegador e editor de arquivos.

Essas pendências não descrevem a árvore atual.
