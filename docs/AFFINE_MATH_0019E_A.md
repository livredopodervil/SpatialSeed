# Matemática afim isolada 0019e-a

Pacote independente de Three.js, DOM, renderer, editor e Sandbox.

A cópia de índice `k` recebe `M_k = T^k M`.

Esta etapa modifica apenas o pacote matemático, a suíte `affine-math`,
o registro da suíte no console e o identificador de build. Não altera
duplicação, comandos editoriais ou parser de `duplicate`.
