export class ModuleRegistry{#modules=new Map();#failures=new Map();register(m){if(!m?.manifest?.id)throw new TypeError('manifest.id required');if(this.#modules.has(m.manifest.id))throw new Error('duplicate module');this.#modules.set(m.manifest.id,m)}async activateAll(ctx){for(const[id,m]of this.#modules){try{await m.activate(ctx)}catch(e){this.#failures.set(id,e);console.error('module disabled',id,e)}}}describe(){return[...this.#modules.values()].map(m=>({...m.manifest,failed:this.#failures.has(m.manifest.id)}))}}