# Spatial Seed — semente inicial

Este arquivo será regenerado por:

```bash
bash tools/seedctl seed
```

Ele registra ambiente, arquitetura, estado Git, hashes, comandos e protocolo de continuidade para uma nova instância de LLM.
