export class OutlineRenderer{constructor(target){this.target=target}update(region,sandbox,modules){const objects=sandbox.getState().objects.map(o=>`<div class="object-row"><strong>${esc(o.name)}</strong><br>${esc(o.kind)} · [${o.position.join(', ')}]</div>`).join('');const mods=modules.map(m=>`<div class="object-row">${esc(m.id)} — ${m.failed?'desativado':'ativo'}</div>`).join('');this.target.innerHTML=`<div class="region-card"><strong>${esc(region.descriptor.name)}</strong><br>versão autoritativa ${region.version}<br>sandbox base ${sandbox.baseVersion}<br>${sandbox.dirty?'alterações locais':'sem alterações locais'}</div><div class="region-card"><strong>Objetos</strong>${objects}</div><div class="region-card"><strong>Módulos</strong>${mods}</div>`}}function esc(v){return String(v).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;')}